#include <client.h>


// void mx_sticker(t_info *info) {
//     t_chat_window *chat = info->widgets->s_chat_window;



// 	chat->win_stick = gtk_window_new (GTK_WINDOW_TOPLEVEL);
// 	gtk_window_set_title (GTK_WINDOW(chat->win_stick), "win_stick");
// 	gtk_window_set_resizable (GTK_WINDOW(chat->win_stick), FALSE ); // чтоб пользователю нельзя было менять размер окна
// 	//g_signal_connect(GTK_WIDGET(chat->win_stick), "destroy", G_CALLBACK(gtk_main_quit), NULL);
   
// 	chat->button = gtk_button_new_with_label("stickers_button");
// 	g_signal_connect(GTK_BUTTON(chat->button), "clicked", G_CALLBACK(button_clicked), NULL);

// 	//gtk_container_set_border_width(GTK_CONTAINER(chat->win_stick), 10);
// 	gtk_container_add(GTK_CONTAINER(chat->win_stick), chat->button);
// 	//gtk_widget_show_now (chat->win_stick);
//     gtk_widget_show_all(chat->win_stick);

    




    // gtk_window_get_position(GTK_WINDOW(widge->widgets->s_chat_window->s_chat_window),
    // 									 &widge->window_x, &widge->window_y);
    // gtk_window_move(GTK_WINDOW(widge->widgets->s_chat_window->win_stick),
    // 									widge->widgets->s_chat_window->window_x,
    // 							 		widge->widgets->s_chat_window->window_y + 520);
    // if (widge->widgets->s_chat_window->on_sticker == 0) {
    //     widge->widgets->s_chat_window->on_sticker = 1;
    //     gdk_threads_add_idle ((GSourceFunc) mx_idle_showall, widge->widgets->s_chat_window->win_stick);
    //     // (widge->win_stick);
    // }
    // else {
    //     hide_win(widge);
    // }
// }
